module.exports = (interaction, commandObj, handler, client) => {
    // This now allows everyone to use the command, no matter where they use it
    return false; // Allow the command to proceed
  };
  